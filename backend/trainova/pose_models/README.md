# MediaPipe model file

Place `pose_landmarker.task` in this folder for real landmark extraction.

Recommended source: Google MediaPipe Pose Landmarker task model from the official Google AI Edge MediaPipe documentation.

The backend will still run without the model using a safe fallback score, but production pose scoring needs this model file.
